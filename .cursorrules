DWP Frontend 개발 표준 가이드 (FINAL · 통합본)

이 문서는 DWP Frontend의 단일 기준(Single Source of Truth)이다.
Cursor, 신규 개발자, 외주 인력 모두 이 규칙을 최우선으로 따른다.

1. 프레임워크 및 UI 스택 (🔥 핵심 기준)
Core

React 18+

TypeScript (Strict Mode 필수)

UI / Design System

Material UI (MUI) v5를 유일한 핵심 UI 라이브러리로 사용한다

Minimal UI Kit 템플릿을 디자인 및 구조의 표준으로 삼는다

shadcn/ui, Radix UI 기반 컴포넌트는 사용하지 않는다

(실험/PoC를 제외하고 운영 코드에서 금지)

Styling

기본 스타일링은 MUI System (sx, styled) + Emotion을 사용한다

Tailwind CSS는 다음 용도로만 보조적으로 허용한다:

layout 보조 (flex, grid, gap)

spacing 미세 조정

Tailwind로 UI 컴포넌트(Button, Card 등)를 직접 구현하는 것은 금지

Icons

Iconify (@iconify/react)를 표준 아이콘 시스템으로 사용

lucide-react, heroicons 등은 사용하지 않는다

Theme

Minimal UI Kit의 createTheme + ThemeContext 구조를 유지

Light / Dark Mode 기본 지원

모든 Remote 앱은 Host의 ThemeProvider를 반드시 상속한다

2. 아키텍처 원칙 (Nx + Micro Frontends)
구조

Nx Monorepo 기반

Webpack Module Federation 사용

역할 분리 (아주 중요)
Host (Shell)

전체 레이아웃의 유일한 소유자

Sidebar

Top Navigation / Header

Global Settings

글로벌 UI 상태 관리

메뉴 구성 및 권한 기반 노출 제어

Remote Module 로딩 및 Content Slot 제공

Remote (Module)

비즈니스 콘텐츠만 담당

Sidebar / Header / Layout 직접 구현 또는 수정 금지

Host가 제공한 Content Slot 안에서만 렌더링

Shared

libs/design-system

Minimal UI Kit에서 추출한 모든 공통 컴포넌트

Theme, Typography, Assets, 공통 Hooks

libs/shared-utils

axiosInstance

tenant / auth / env 유틸

3. 코드 작성 규칙
기본

모든 파일은 TypeScript

any 타입 사용 절대 금지

컴포넌트는 반드시 화살표 함수 사용

export const Component = () => { ... }

구조

비즈니스 로직은 Custom Hook으로 분리

UI 컴포넌트는 순수 렌더링 책임만 가진다

네이밍

컴포넌트: PascalCase

폴더 / 일반 파일: kebab-case

4. 상태 관리 및 데이터 통신
State Management

Global UI State:

Theme Mode

Sidebar Collapse

Active Menu

→ Zustand 또는 템플릿 Context 사용

Server State:

TanStack Query 필수 사용

API

libs/shared-utils/axiosInstance만 사용

모든 API는 interface / type 정의 필수

Backend ApiResponse<T> 규격을 기준으로 매핑

5. Sidebar & 메뉴 아키텍처 (🔥 이번 논의 핵심)
Sidebar 소유권

Sidebar는 Host(Shell)의 전용 책임

Remote는 Sidebar UI를 직접 그리지 않는다

메뉴 구성 방식

Sidebar 메뉴는 정적 메뉴 + 동적 메뉴로 구성된다.

1) Static Menus (Host 고정)

App, Analytics, Banking 등

항상 존재하는 기본 메뉴

2) Dynamic Menus (Remote / Backend 기반)

Backend API 응답으로 결정

사용자 권한 / 앱 사용 여부에 따라 노출

Remote 앱은 메뉴 메타데이터만 제공할 수 있다

interface SidebarMenu {
  id: string
  label: string
  icon: string
  route: string
  source: 'host' | 'remote'
  enabled: boolean
}


Sidebar는 이 데이터 모델만 렌더링한다

6. 멀티 테넌시 & 인증
Multi-tenancy

고객사는 서브도메인으로 식별

client-a.dwp.com

window.location.hostname 기반 tenant 추출 유틸 필수 사용

Auth

진입 시 Backend로부터 테넌트 인증 전략 조회

SSO / 자체 로그인 여부와 무관하게

최종 결과는 동일한 User + JWT 모델로 통합

모든 라우트는 AuthGuard 통과 필수

7. 빌드 및 환경 관리

환경 변수:

process.env.NX_API_URL

Gateway 단일 진입점:

/api/main/**

/api/mail/**

인프라 설정 파일 수정 시 영향도 분석 필수

포트 규칙 엄격 준수

8. 테스트 및 품질

Custom Hooks / Utils:

Vitest 단위 테스트 작성

UI 컴포넌트:

Storybook 스토리 필수

Shell 레이아웃:

스냅샷 테스트 권장

9. 에셋 관리

공통 에셋:

libs/design-system/src/assets

Host 전용 정적 리소스:

apps/dwp/public

10. 금지 사항 (명시적)

shadcn/ui, Radix UI 컴포넌트 사용 ❌

Sidebar / Header를 Remote에서 직접 구현 ❌

MUI 대신 div + Tailwind로 UI 컴포넌트 구현 ❌

ThemeProvider 중복 선언 ❌