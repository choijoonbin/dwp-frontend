# R2 R3 출시 증거 실행 가이드

- 상태: 실행 Harness 완료, 운영·고객 증거 대기
- 기준일: 2026-08-13
- 원장: `release-evidence/release-readiness.json`
- 내부 검사: `corepack yarn release:evidence:check`
- 출시 차단 검사: `corepack yarn release:gate`

## 1. 상태 원칙

내부 자동 테스트는 출시 준비의 필요조건이지만 실제 고객 규모, 보조기술 사용자, 운영
Topology와 책임자의 승인을 대신하지 않는다. 원장은 `COMPLETE`, `BLOCKED_EXTERNAL`,
`FEATURE_DISABLED`만 허용한다. 외부 기능은 승인이 없으면 실행하지 않고 fail-closed
근거를 가져야 한다.

`release:evidence:check`는 ID 누락, 중복, 빈 Owner·Blocker, 존재하지 않는 증거와 거짓
완료를 CI에서 차단한다. `release:gate`는 `R2`, `R3`, `A`의 필수 항목이 모두
`COMPLETE`가 아니면 종료 코드 2로 실패한다.

## 2. R2 실행 계약

| Gate  | 실행                                                                             | 필수 증거                                                  |
| ----- | -------------------------------------------------------------------------------- | ---------------------------------------------------------- |
| R2-01 | 승인된 최대 Tenant Fixture로 조직 Graph, People Grid, Catalog, Audit를 반복 측정 | P50/P95/P99, 렌더·상호작용, Heap, 가상화 정확성, 예산 승인 |
| R2-02 | DB·Redis·Queue·Adapter 중단, 지연, 중복, 순서 역전과 재시작 주입                 | 데이터 손실·중복, Backoff, Replay, Compensation, RTO       |
| R2-03 | 실제 200% Browser Zoom, Keyboard-only, VoiceOver, NVDA, JAWS                     | WCAG 2.2 Journey 점검표, 결함·재시험, 승인자               |
| R2-04 | `D-10`에서 승인된 Browser/OS 조합으로 기능·시각 기준선 실행                      | Browser Version, OS, Baseline 승인·갱신 이력               |
| R2-05 | 구성원·Tenant Admin·Provider Operator의 대표 과업 관찰                           | 성공률, 시간, 오류, 재탐색, SUS 또는 합의 지표, 개선 조치  |

WCAG 2.2의 Keyboard, Reflow, Resize Text와 Focus 기준을 사용하고 자동 Axe 결과만으로
수동 보조기술 검증을 대체하지 않는다. Browser Matrix는 Playwright Project로 실행하되
실제 제품 지원 Matrix를 제품·지원 조직이 먼저 승인한다.

## 3. R3 실행 계약

| Gate  | 실행                                                                  | 필수 증거                                              |
| ----- | --------------------------------------------------------------------- | ------------------------------------------------------ |
| R3-01 | 운영 Proxy Chain, Secure Cookie, Session/JWT Key Rotation과 폐기 시험 | 위협 모델, 신뢰 CIDR, Rotation·Revocation, 침투 시험   |
| R3-02 | Workload Identity, KMS/HSM, WORM, SIEM, RLS와 Alert 연결              | Tenant 누출 0건, Key 장애·복구, 불변 보존, SIEM 재전송 |
| R3-03 | OpenTelemetry Collector HA, Sending Queue와 Persistent WAL 장애 주입  | Queue 용량, Backpressure, Drop 허용치, 재전송, Alert   |
| R3-04 | 실제 Provisioner Preview·승인·실행·Compensation·Ring 승격             | Idempotency, Rollback, 외부 SLI, 고객 영향·통지 감사   |
| R3-05 | 승인 규모 Load·Soak·Backup Restore·배포 Rollback                      | SLO, RTO, RPO, 정합성 Report와 Release Owner 승인      |

OpenTelemetry 네트워크 Exporter는 Sending Queue와 Retry를 사용하고, Collector 재시작 중
손실을 허용하지 않는 경로는 Persistent Storage 또는 승인된 Message Queue를 사용한다.
Queue Full, Disk Full, Backend 장기 중단과 인증 Context 손실을 각각 시험한다.

## 4. 증거 등록 절차

1. 민감 로그 원문 대신 승인된 증거 저장소의 HTTPS Link와 비민감 요약을 남긴다.
2. Evidence에는 실행 Commit, 환경, Dataset 규모, 시간, 도구 Version과 결과를 포함한다.
3. 결함이 있으면 Gate를 `COMPLETE`로 바꾸지 않고 재시험 Link를 추가한다.
4. Owner와 승인자가 결과를 확인한 뒤 해당 원장 항목만 `COMPLETE`로 변경한다.
5. `corepack yarn release:evidence:check`와 `corepack yarn release:gate`를 실행한다.
6. Release Artifact의 SHA-256·서명·SBOM과 배포 대상 Commit을 함께 보존한다.

NIST SSDF의 Release Integrity 원칙에 따라 Source 검증과 실제 Release Artifact 무결성
정보를 연결한다. 로컬 테스트 Screenshot이나 임의 서명 문구를 운영 승인으로 사용하지 않는다.

## 5. 현재 판정

- R0·R1 내부 구현: 완료
- R2 출시 검증: 외부 규모·사용자·보조기술 증거 대기
- R3 운영 Hardening: 운영 Topology·보안 인프라·Adapter 증거 대기
- D-01~D-17: 미승인 기능은 fail-closed 또는 비활성
- A-01~A-05: 실제 책임자 승인 대기

따라서 현재 저장소는 내부 개발 기준선을 검증할 수 있지만 Production Release는
`release:gate`가 의도적으로 차단한다.
