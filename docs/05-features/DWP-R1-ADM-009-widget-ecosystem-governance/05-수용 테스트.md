# DWP-R1-ADM-009 수용 테스트

> 현재 상태: Phase 1A fixture·receiver 계약 일부 완료, Runtime·API 구현과 자동 증거 수집은 단계별 진행 중.
> 체크되지 않은 항목은 출시 증거가 아니다.

2026-08-29 현재 자동 증거 정의는 대상 Model Unit 34건, Feature On Playwright 13건(Catalog 6, Resilience 4,
Tenant·Provider Governance 3), Feature Off 3건, Home Library
1440/1280/390/320 Screenshot·Axe, 200% Dialog 내부 Control/마지막 행, Keyboard 진입·종료 Focus,
Hide/Restore Persistence, 단일 Live Region, Forced Colors 선택 상태, Tenant/Provider 정적 Build Catalog의
200%·320 상세 접근·키보드 목록→상세 포커스, Home Experience의 wide `1920×312`·desktop
`1440×326`·tablet `1024×396`·mobile `390×340` 실제 비율 Preview다. Resilience 4건은 통합 정본
`15b2137`에서 500 재시도, 409 Draft 보존·재적용, 320px Keyboard·Axe 회귀를 고정한다.
이는 아래 전체 수용 기준이나 Backend·보안·운영 승인을 대신하지 않는다.

## 1. Phase 0 Frontend Vertical Slice

- [x] `AT-01` 기본 Home에서 Library가 빈 Dialog가 아니라 전체 유효 App·Widget을 표시한다.
- [ ] `AT-02` 이미 배치된 항목은 `홈에 있음`, 숨긴 항목은 `복원`, 미배치 항목은 `추가`로 구분된다.
      상태 Model은 검증됐고 실제 v5 Journey에서는 `ADDED/RESTORE`만 도달했다.
- [x] `AT-03` 모든 항목이 배치된 경우 `지원되는 모든 항목이 이미 홈에 있습니다`와
      유효한 다음 행동을 표시한다.
- [ ] `AT-04` Hide → Save → Reload → Library/복원 → Save → Reload 후 순서·표시와 Version이
      유지된다. 표시 상태 Persistence는 자동화됐고 순서·Version 명시 검증은 남아 있다.
- [x] `AT-05` Flow에서 관리형 `command-rail/Now`가 개인 추가·숨김 대상으로 나타나지 않는다.
- [ ] `AT-06` Search·Filter 결과 없음과 Catalog Error·Capability Off가 서로 다른 상태다. Search·Filter
      Empty와 Feature Off는 자동화됐고 독립 Catalog Error Source는 아직 없다.
- [ ] `AT-07` Gallery Cancel·Save Failure·409 후 기존 Home과 Draft를 잃지 않는다. 500 실패 뒤 Draft
      보존·재시도·Reload와 409 닫기·재적용·최신 Version 저장은 자동화됐고 Gallery Cancel과 409 최신 Home
      내용 보존의 명시 검증은 남아 있다.
- [ ] `AT-08` 기존 Home Studio·HomeTemplate·복수 Home 기능 Flag 계약이 회귀하지 않는다.
- [x] `AT-09` Feature Flag Off에서는 `항목 추가`나 `홈에 항목 추가`를 노출하지 않고 숨긴 항목 복원만
      제공한다. 복원 대상 0의 `aria-disabled` 사유, 강제 Click 무동작, Hide 후 활성화, 마지막 Restore 후
      Trigger Focus 복귀를 자동화했다.

## 2. Definition·Instance

- [ ] `AT-10` Provider가 게시하지 않은 Definition은 일반 Effective Catalog에 나타나지 않는다.
- [ ] `AT-11` 같은 Definition을 서로 다른 Filter로 두 Instance 배치하고 독립 저장·삭제할 수 있다.
- [ ] `AT-12` 알 수 없는 Definition·Version·Renderer는 임의 실행하지 않고 `INCOMPATIBLE` 또는
      `REVOKED` 상태로 Fail Closed 한다.
- [ ] `AT-13` Manifest Field·Filter·Item Limit 밖의 Config는 Server가 `400`으로 거부한다.
- [ ] `AT-14` Widget Runtime 장애가 Header·App Dock·다른 정상 Widget과 Home 편집을 막지 않는다.
- [ ] `AT-15` Widget 내부에 같은 축의 세로 Scroll이 없고 초과 데이터는 `전체 보기`로 연결된다.
- [x] `AT-16` Phase 0 정적 Lifecycle은 `BLOCKED`를 기존 표시·신규 추가·복원에서 모두 제외하고,
      `DEPRECATED`는 이미 표시 중인 항목만 유지하며 신규 추가·복원·기본 보충을 차단한다. 동적 Provider
      Lifecycle과 Server 권한원은 후속 범위다.

## 3. Provider·Tenant·User 권한

- [ ] `AT-20` Definition 작성자는 자기 App 범위만 수정한다.
- [ ] `AT-21` 독립 Reviewer 승인 없이 고위험 Definition을 게시할 수 없다.
- [ ] `AT-22` Tenant는 Provider Quarantine·Data Class·필수 Permission을 완화할 수 없다.
- [ ] `AT-23` User Share Grant는 App·Widget·원천 데이터 Permission을 부여하지 않는다.
- [ ] `AT-24` Audience에서 제외된 사용자는 Browser Payload·DOM·접근성 이름·Telemetry에 민감한
      Definition·Data를 받지 않는다.
- [ ] `AT-25` Tenant Admin Explain 응답도 자기 Tenant 범위만 조회한다.
- [ ] `AT-26` Frontend 숨김·Display Condition을 우회해 직접 API를 호출해도 Server가 거부한다.

## 4. 현재 사용자 Data Binding·Privacy

- [ ] `AT-30` A가 만든 Preset을 B가 적용하면 B의 Entitlement와 데이터만 표시된다.
- [ ] `AT-31` Preset·Blueprint·Instance Snapshot에 A의 User ID, 업무 본문, Object ID, Token,
      Credential이 없다.
- [ ] `AT-32` `MY_TEAM` Binding은 Manager 권한과 현재 Org Graph가 없으면 Count·제목 없이 거부된다.
- [ ] `AT-33` Cache가 Tenant·User·Permission Context를 넘어 재사용되지 않는다.
- [ ] `AT-34` Widget Action은 목적 Domain에서 현재 사용자 권한을 다시 확인한다.
- [ ] `AT-35` Telemetry는 관리형 Key·상태·Duration Bucket만 받고 제목·Query·URL·사용자 ID를 거부한다.

## 5. Lifecycle·Version·Revocation

- [ ] `AT-40` Published Definition Version은 수정되지 않고 변경은 새 Version을 만든다.
- [ ] `AT-41` Major/Config Schema 변경은 영향 Preview·Migration·승인 없이 승격되지 않는다.
- [ ] `AT-42` Preset 회수는 신규 Apply를 막지만 기존 개인 사본을 조용히 삭제하지 않는다.
- [ ] `AT-43` Provider Security Revocation은 다음 Render에서 데이터 노출을 중단하고 Replacement·제거를
      제공한다.
- [ ] `AT-44` Tenant Disable은 해당 Tenant만 차단하고 다른 Tenant에 영향을 주지 않는다.
- [ ] `AT-45` Rollback은 새 Revision·Audit Event를 만들고 이전 Revision을 수정하지 않는다.
- [ ] `AT-46` Event Consumer 지연 중에도 Runtime의 현재 정책 재검사가 Revoked Definition 실행을 막는다.
- [x] `AT-47` Tenant Admin의 기존 Home Blueprint 게시·회수는 revision, audience, widget 수, 기존 개인
      Home 영향과 복구 방법을 확인한 뒤에만 실행할 수 있다. 이는 기존 HomeTemplate API의 정적·정성적
      확인이며 Tenant Policy의 authoritative server `impactRevision` 구현 증거는 아니다.

## 6. Preset·Blueprint 공유

- [ ] `AT-50` Private Preset은 Owner 외 목록에 나타나지 않는다.
- [ ] `AT-51` Direct·Group 공유는 명시적 수신자와 Tenant 경계를 검증한다.
- [ ] `AT-52` Tenant Library 게시에는 자동 Schema Scan과 관리자 승인 결과가 있다.
- [ ] `AT-53` 적용 전 요구 App·Permission·Version과 누락 항목 Diff를 표시한다.
- [ ] `AT-54` 기본 적용은 새 개인 사본이며 원본 업데이트가 자동 덮어쓰지 않는다.
- [ ] `AT-55` Home Blueprint가 현재 Home을 교체할 때 별도 Preview·확인·복원 Revision이 있다.
- [ ] `AT-56` Cross-tenant 사용자 Preset 공유를 Server가 거부한다.

## 7. Responsive·접근성

- [x] `AT-60` 1440×900과 1280×800에서 첫 Viewport에 검색·Filter·실제 항목·주 행동이 보인다.
- [x] `AT-61` 390×844와 320×568에서 Full-screen Dialog가 수평 Overflow 없이 동작한다.
- [x] `AT-62` 실제 200% 글자 크기에서 Dialog 내부 폭·보이는 Control 경계·마지막 행 도달과
      Tenant/Provider 수평 Overflow가 없다.
- [ ] `AT-63` Keyboard만으로 Library 진입, 검색, Tab·항목 이동, 추가·복원, 닫기와 저장이 가능하다.
      Enter 진입·검색 초기 Focus·Keyboard 복원·Escape 종료 Focus, 500 재저장 Enter와 409 Dialog Action Tab
      순서·재적용은 자동화됐다. 검색 입력·Library 내 Tab/항목 이동·신규 Add부터 변경 Draft 저장까지의 전 구간
      Keyboard Fixture는 남아 있다.
- [ ] `AT-64` 추가·복원 후 Focus가 대상 항목 또는 안전한 Heading으로 이동하고 `aria-live`가 결과를
      알린다. 복원 뒤 포커스 가능한 빈 상태·Live Region과 Dialog 종료 Trigger Focus는 Click·320px Keyboard
      Journey에서 자동화됐고 신규 추가 Focus·Announcement Fixture가 남아 있다.
- [ ] `AT-65` Light·Dark·High Contrast에서 상태·선택·Focus가 색에만 의존하지 않는다. Home Gallery의
      Forced Colors 라벨·선택 Outline·Axe는 검증됐고 Tenant/Provider 전체 수동 고대비 검토는 남아 있다.
- [ ] `AT-66` Reduced Motion에서 반복 Jiggle·Pulse·위치 Animation이 없고 의미가 유지된다.
- [ ] `AT-67` 한국어·영어 긴 Label, Screen Reader 이름, 44×44px Target을 검증한다. 대표 Locale,
      객체별 이름과 모바일 Filter·닫기·Footer·Reset의 실측 Target은 검증됐고 의도적인 최장 Label
      Fixture는 남아 있다.
- [x] `AT-68` Admin Home Experience의 wide·desktop·tablet·mobile 선언 해상도가 실제 Frame 비율과
      동일하고, 동일 논리 Canvas에서 배경 crop·focal point·콘텐츠가 균일 축소되며 양축 Overflow가 없다.
      320px에서도 WIDE와 MOBILE focal point가 실제 브라우저 media query와 독립적으로 전환되고 세 Preview
      Control 그룹의 Target이 모두 44×44px 이상이다.

## 8. 성능·운영

- [ ] `AT-70` Catalog Search·Filter가 불필요한 Widget별 Network 요청을 만들지 않는다.
- [ ] `AT-71` Runtime Broker가 같은 Source 요청을 Batch/Deduplicate하고 Widget별 무제한 Polling을
      허용하지 않는다.
- [ ] `AT-72` Catalog 실패가 기존 Home, App 실행과 Now의 첫 업무 Action을 차단하지 않는다.
- [ ] `AT-73` Definition·Tenant·Preset 변경에 Correlation, Actor, Diff, Approval과 Version이 남는다.
- [ ] `AT-74` Feature Flag Off 시 기존 Preference Hash와 Home 동작이 보존된다. Home/Tenant/Provider의
      기존 Surface 복귀 3건은 자동화됐고 저장 전후 Preference byte/hash 비교는 남아 있다.
- [ ] `AT-75` Pilot 확대 전 Kill Switch, On-call, Replacement와 Rollback Runbook을 실행한다.
- [ ] `AT-76` Active Rollout Approval 만료 시 scheduler 지연/Flag 지연 중에도 다음 evaluation은 DB clock으로
      observer를 중단하고, deterministic expiry command가 한 번만 `EXPIRED/STATIC` 원장을 기록한다.
- [ ] `AT-77` 위조·unknown-kid Bootstrap assertion은 JTI를 소비하지 않고 같은 JTI의 정상 assertion은 한 번
      성공하며, 그 이후 재사용만 replay로 거부된다.
- [ ] `AT-78` fresh Provider client가 Release Channel GET에서 current/previous head와 optimistic version을 얻어
      promote/rollback을 시작할 수 있다.
- [ ] `AT-79` REJECTED/QUARANTINED Version Detail은 후속 rework/clearance에 필요한 current producer ID가
      non-null이고 다른 state에서는 stale ID를 반환하지 않는다.
- [ ] `AT-80` Promotion Gate는 threshold 경계값을 허용하고 sample·diff·safety·queue·outbox·drill 각 한계
      초과 mutation을 독립적으로 거부한다.
- [ ] `AT-81` Runtime control Event의 target type/resulting state/reason과 Rollout stop cause/reason이 바뀐
      조합은 schema와 독립 verifier 모두 거부한다.
- [x] `AT-82` Command/Evidence-expiry Event의 V1 projection type·locked-row JCS hash·CREATE null sentinel과
      Runtime enable/Quarantine clear child Approval의 ID·locked revision·소비 command 결속을 30종/positive
      24/negative 54 독립 verifier가 검증하고 Shadow에는 projection field가 없다.
- [x] `AT-83` Tenant Policy Publish는 deterministic `expectedImpactRevision`을 요구하며 stale head, count 변화,
      cross-tenant binding, authority 만료를 모두 `409 IMPACT_REVISION_CONFLICT`/0 mutation으로 검증한다.
- [x] `AT-84` Registry/Tenant Policy/Safety head는 state change/rollback만 `+1`, replay/no-op/failure는 0,
      BIGINT overflow는 readiness fail로 검증한다.
- [x] `AT-85` Pagination head conflict, Effective 세 head 재확인 2회 후 503, Safety-before-positive-cache,
      Rollout head tuple·연속 ACTIVE window를 독립 verifier가 검증한다.
- [x] `AT-86` Completion은 Runtime enable과 Quarantine clear의 durable request·locked current head·child
      Approval을 exact 결속하고 `ACTIVE`·미사용·미만료에서 동일 command의 `CONSUMED` 전이만 허용한다.
      21 identity/21 success/2 terminal/96 negative mutation을 독립 verifier가 검증한다.
- [x] `AT-87` Widget internal receiver는 12개 exact Route·GET의 실제 HEAD method, dual ES256 proof,
      request-target·JCS body·command/seal semantic claim을 replay 전에 exact 결속한다. Generic provisioning
      token, browser authority header, ambiguous path, unsafe number·Unicode·duplicate JSON, 다중 Trust adapter,
      command/seal substitution, 4종 target hash substitution과 header enumeration 불능을 Widget receiver
      61건과 Provisioning 5건, 총 66건의 격리 Snapshot 자동 회귀로 거부한다.
- [ ] `AT-88` Production ES256 verifier·pinned JWKS/key rotation, 정확히 한 개의 durable atomic replay store,
      direct TLS/mTLS, Controller·Receipt/Gate/Ledger·KMS seal을 실제 환경에서 연결하고 이미 구현된 21개
      closed payload branch validator를 Schema·Golden과 동일하게 유지하며 authoritative domain ownership,
      current-state·SoD와 outage·forged forwarded-proto Negative를 통과한다. 미완료 동안 실제 receiver는
      `503 TRUST_UNAVAILABLE`로 fail closed하며 production 활성화를 금지한다.
- [x] `AT-89` Gateway는 `/api/platform/internal{,/**}`를 upstream 전달 전에 exact 404로 닫고 Provider
      provisioning token/header도 외부에서 Platform에 전달하지 않는다. Gateway 285건과 Widget 최종 동결
      소스를 포함한 fresh Backend 전체 `check` 72/72 Task가 격리 Snapshot에서 통과했다.
- [x] `AT-90` Widget security Java enum·semantic target contract를 중앙 Registry와 exact 수렴한다. V206은
      `TargetContract` 8값, V207은 `WidgetRegistryIngressFailure` 12값과 revision 4를 순방향으로 등록하며
      metadata·unexpected value·revision·target row·binding drift를 fail closed한다. PostgreSQL contract
      18건과 Receiver·Provisioning 66건, 총 84건, Platform 전체 774건, Backend 전체 72/72 actionable Task,
      전체 code-contract audit 770 contract·3,008 active value·904 binding을 통과하고 managed V207 clean
      boot health 200을 확인한다.
- [x] `AT-91` Widget Receiver activation interlock은 canonical 설정 누락/`false`에서 항상 503으로 차단하고,
      현재 Milestone의 `true`와 잘못된 Boolean은 Platform 초기화를 실패시킨다. Filter는 설정과 무관하게
      component scan으로 등록되며 실제 Spring wiring에서도 verifier·body·replay·downstream 호출 0을
      Widget 대상 78건으로 검증한다. 동일 production snapshot의 Platform 전체 `check` 860/860도 통과했다.
      이는 production permit 또는 Release 승인이 아니다.

## 9. 자동 증거 Matrix

| Suite               | 필수 증거                                                       |
| ------------------- | --------------------------------------------------------------- |
| Frontend Unit       | Catalog 상태 계산, 검색·Filter, 추가됨·복원, Flow 관리형 제외   |
| Component           | Empty·Error·Permission·Long label·Focus·Keyboard                |
| API Contract        | Manifest·Effective Catalog·Instance·Preset·409·Revocation       |
| Backend Integration | Tenant/User Scope, Source ACL, Cache 격리, Event 지연           |
| Playwright Desktop  | Edit → Library → Hide/Restore → Save → Reload                   |
| Playwright Mobile   | 390·320 Full-screen Library와 Keyboard/Touch 대체               |
| Accessibility       | Axe Critical·Serious 0, 수동 Screen Reader·High Contrast        |
| Migration           | v5→v6 Shadow Hash, Classic↔Flow, Flag On↔Off                    |
| Operations          | Ring Rollout, Tenant Disable, Global Revocation, Rollback Drill |

## 10. Release Gate

- [ ] Product Owner: 문제·용어·MVP·성공 지표 승인
- [ ] App Platform: Definition·Manifest·SDK 계약 승인
- [ ] Tenant Admin Owner: 정책·Audience·SoD 승인
- [ ] Security·Privacy: Sandbox, Binding, Cache, Telemetry, Revocation 승인
- [ ] Design·Accessibility: User·Tenant·Provider 대표 Journey 승인
- [ ] Backend·Frontend: OpenAPI·Generated Client·Migration·Compatibility 승인
- [ ] Operations: Pilot, SLO, On-call, Kill Switch·Rollback 승인
