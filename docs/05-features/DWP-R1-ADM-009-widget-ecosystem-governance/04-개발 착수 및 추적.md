# DWP-R1-ADM-009 개발 착수 및 추적

## 1. 상태 표기 원칙

- `implemented`: Source와 대표 Journey 자동 증거가 모두 존재
- `in-development`: 구현 또는 검증 중이며 Release 근거로 사용할 수 없음
- `planned`: 승인된 계약 후보이나 Source가 없음
- `future`: Backend·보안·운영 선행조건이 필요한 확장

이 문서 작성 자체는 기능 구현 완료가 아니다.

## 2. 단계별 Backlog

### Phase 0 — Catalog Discovery Vertical Slice

목표: 사용자가 `0`의 의미를 오해하지 않고 현재 유효 항목을 탐색·복원한다.

| ID               | 작업                                                            | 상태             | 완료 증거           |
| ---------------- | --------------------------------------------------------------- | ---------------- | ------------------- |
| `ADM009-FE-001`  | Gallery 입력을 전체 Entitled App·Widget과 Placement 상태로 분리 | `implemented`    | Model·E2E Test      |
| `ADM009-FE-002`  | 기본 `라이브러리`와 App·Widget 통합 `숨긴 항목` IA 분리         | `implemented`    | 1440·390·320 증거   |
| `ADM009-FE-003`  | `추가/복원/홈에 있음` 상태와 Search·Type·Status Filter          | `in-development` | ADD는 Model만 검증  |
| `ADM009-FE-004`  | 원인별 Empty·Error·Capability 상태                              | `in-development` | Partial/Error Test  |
| `ADM009-FE-005`  | Hide → Save → Reload → Restore → Save → Reload Journey          | `implemented`    | Playwright          |
| `ADM009-FE-006`  | 실제 Server `LOCKED/FORBIDDEN` Source와 안전한 이유             | `planned`        | Effective API 필요  |
| `ADM009-FE-007`  | 기존 HomeTemplate 게시·회수 전 정적 영향·복구 확인              | `implemented`    | Playwright·Snapshot |
| `ADM009-FE-008`  | 정적 Lifecycle의 기존 표시·신규 추가·복원 Fail-closed 정책      | `implemented`    | Model Unit          |
| `ADM009-FE-009`  | 모바일 목록→상세 포커스와 4단계 Preview 비율·논리 Canvas 계약   | `implemented`    | Playwright          |
| `ADM009-FE-010`  | Flag Off 복원 명칭·빈 상태 비활성 사유·Trigger Focus            | `implemented`    | Playwright          |
| `ADM009-DOC-001` | 용어·3평면·Backend 경계 문서화                                  | `implemented`    | 이 Package          |

이번 착수에서 구현된 Frontend 범위는 다음으로 제한한다.

- 기존 5개 Native Widget의 실행 계약을 정적 Catalog Seed로 통합하되 사용자 화면에는 내부 Source와
  권한 Key를 노출하지 않음
- Home Add Dialog에서 전체 Entitled App·Widget과 `추가/복원/홈에 있음` 상태를 표시하고 Library를
  매 진입 기본 View로 사용
- App·Widget 유형 Filter, 상태 Filter, NFKC Search, 결과 수, 구분된 Empty·Feature-off 상태
- 추가·복원 후 Focus 이동과 `aria-live`, Dialog 종료 후 Trigger Focus 복원
- 기존 Tenant Home Composition 안의 Catalog·Home Blueprint 탭과 Provider Code Contract 안의
  Widget Catalog 탭 추가
- Tenant Blueprint만 기존 실제 API를 사용하며, 정적 Build Catalog는 인증·정책·회수 기능이 아닌
  읽기 전용 개발 Preview로 명시
- 기존 Home Blueprint 게시·회수 전에 revision, audience, widget 수, 기존 개인 Home 영향과 복구
  경로를 명시하고 명시적 확인을 요구. 이는 기존 HomeTemplate API의 정적·정성적 확인이며 향후
  Tenant Policy의 authoritative server `impactRevision` 구현을 뜻하지 않음
- 정적 Catalog에서 `BLOCKED`는 기존·신규·복원 모두 제외하고, `DEPRECATED`는 이미 표시 중인 항목만
  유지하며 신규 추가·복원·기본 보충을 차단
- Tenant·Provider 모바일 목록에서 키보드로 항목을 선택하면 상세 제목으로 포커스와 viewport를 이동하고,
  Home Experience Preview는 wide `1920×312`·desktop `1440×326`·tablet `1024×396`·mobile
  `390×340`의 선언 비율과 논리 Canvas를 단일 기준으로 사용
- Catalog Flag가 꺼지면 `항목 추가`·`홈에 항목 추가` 약속을 노출하지 않고 `숨긴 항목 복원` 경로만
  제공한다. 복원 대상이 0이면 Trigger는 포커스 가능한 `aria-disabled` 상태와 사유를 제공하며, 항목을
  숨기면 즉시 활성화되고 마지막 복원 뒤에도 Dialog 종료 Focus를 안전하게 돌려준다.
- Catalog·배치 Model Unit Test, 1440/1280/390/320 Playwright Screenshot, 200% Overflow, Keyboard,
  Focus·Live Region, Hide/Restore Persistence, Axe Critical·Serious 0 검증

409 충돌 병합 관련 Unit은 `app-launchpad-model` 35건, `app-launchpad-concurrent-order` 6건,
`home-draft-history` 10건으로 합계 51/51을 통과했다. 이 중 신규 20건은 App Group·Folder Member·Widget
순서의 양립 가능한 Local/Latest Pair Delta, Folder 해제·권한 축소, Presentation·Widget 속성 3-way 병합을
고정한다. 2026-08-31 Node 24.19.0에서 Flag On Playwright 14/14(Catalog 6, Resilience 5, Tenant·Provider
Governance 3)와 별도
Flag Off 3/3을 통과했고, 보호 브랜치 CI에도 동일한 14건의 기능·접근성 Gate를 등록했다. Linux CI는
Darwin 시각 Baseline을 기능 Gate로 오인하지 않도록 Screenshot assertion만 제외하고, 기존
Home Experience exact-geometry 1건이 정의돼 기능·반응형·접근성·시각·Rollback 경계를 검증한다.
Resilience 5건은 500·409 Draft 보존·재시도, concurrent 최신 내용의 3-way 재적용, 확인된 편집 취소의
무저장 복구와 320px Keyboard·Axe를 고정한다. 변경 범위 ESLint·TypeScript·source-size도 통과했다.
고정 Frontend Snapshot의 전체 Unit 296개 파일·1,677/1,677과 Workspace Node 24.19.0의 표준 `yarn build`도
통과했다. Build는 Package policy, OpenAPI Sync,
전체 ESLint·TypeScript, App/Route/API/Feature Boundary, Source Size, Design-system, i18n, Display Dictionary,
Vite Production Build와 Bundle Budget을 묶는 최종 Gate다. Backend Migration Owner·전체 Trust 발급/서명
연결·전체 수용 기준은 남아 있으므로 이 Package를 Release-ready로 표시하지 않는다.

Phase 0은 정적 Registry를 동적 생태계로 오인시키지 않는다. 사용자 Widget 제작·공유 CTA는 실제
Capability가 없으면 노출하지 않는다.

### Phase 1A — Control-plane Shadow Bundle

| ID                | 작업                                                               | 상태          |
| ----------------- | ------------------------------------------------------------------ | ------------- |
| `ADM009-DOC-100`  | Manifest·Binding·Tenant Seed·Bootstrap·Rollout·Event 실행 계약     | `implemented` |
| `ADM009-OPS-100`  | 10-rule Shadow Alert Schema·8 Negative·Bootstrap digest/JWS 결속   | `implemented` |
| `ADM009-DOC-101`  | Internal Command 21 branch closed oneOf executable schema          | `implemented` |
| `ADM009-OPS-101`  | Rollout DTO·expiry·Promotion Gate executable schema                | `implemented` |
| `ADM009-DOC-102`  | Completion outcome·status·body·ETag executable schema              | `implemented` |
| `ADM009-DOC-103`  | Event V1 locked projection/JCS hash executable contract            | `implemented` |
| `ADM009-DOC-104`  | Tenant impact TOCTOU·3-head revision authority executable contract | `implemented` |
| `ADM009-BE-101`   | Platform 단일 Definition·불변 Version·Release Channel 원장         | `planned`     |
| `ADM009-BE-102`   | Provider RBAC·SoD 제어 파사드와 전용 Internal Identity             | `planned`     |
| `ADM009-BE-103`   | Tenant default-deny Policy Revision과 기존 Tenant Backfill         | `planned`     |
| `ADM009-BE-104`   | Effective Evaluator·공개/내부 Reason·Shadow Diff                   | `planned`     |
| `ADM009-MIG-105`  | 현재 5개 Legacy Seed·TS/Java Golden Parity                         | `planned`     |
| `ADM009-OPS-106`  | Kill·Quarantine·Revoke·Idempotency·Audit·Outbox·Metric             | `planned`     |
| `ADM009-SEC-107A` | Receiver-first exact Route·Dual-proof·JCS Request Binding Fence    | `implemented` |
| `ADM009-SEC-107B` | Widget Security Enum·Target Contract 중앙 Registry 수렴            | `implemented` |
| `ADM009-SEC-107`  | 명시적 Security Route·Cross-tenant Negative·SoD·Immutability       | `planned`     |

Phase 1A는 Shadow 결과를 관찰하지만 현재 정적 Catalog, Home v5와 Renderer의 권한원이 아니다. 위 항목
중 일부만 연결하거나 Registry 장애 시 정적 Fallback하는 부분 출시는 금지한다.

`ADM009-DOC-100`과 `ADM009-OPS-100`은 JSON Schema·Golden·Negative·독립 digest verifier가 구현됐다는
뜻이며 Backend가 구현됐다는 뜻이 아니다. Event는 30종/positive 24(locked projection 22, projection 없는
Shadow 2)/negative 54, Tenant impact는 positive 1/negative 10, Revision authority는 positive 12/negative 17,
Bootstrap은 ES256
signature와 19 Negative, Alert는 실제 PromQL 10종/8 Negative를 검증한다. Internal Command는
21 branch/21 positive/47 negative mutation, Completion은 21 identity/21 success/2 terminal/96 negative
mutation, Rollout operation·promotion은 9 positive/111 negative case/138 mutation을 검증한다.
Completion response 실행 계약 `ADM009-DOC-102`도 Backend 착수 전 Release Gate다. 2026-08-27 현재
Receiver-first 선행 Slice `ADM009-SEC-107A`는 12개 exact internal Route와 GET의 HEAD alias, 실제 HTTP
method/path/query/JCS body/Idempotency/Correlation 결속, 분리된 ES256 service token·Provider assertion,
command·seal semantic claim, replay 소비 순서, generic provisioning token·browser authority header·ambiguous
path 차단을 구현했다. RFC 8785 공식·경계 Vector, unsafe number·Unicode·duplicate JSON, 다중 Trust adapter,
command/seal substitution, 4종 semantic target hash 재계산, `X-DWP-*` default-deny를 포함한 Widget receiver
61건과 기존 Provisioning 5건, 총 66건이 격리 Snapshot에서 통과했다. JCS 1.1 artifact는 고정 버전과
Gradle SHA-256 verification metadata로 잠갔다.

이는 Registry Backend가 출시됐다는 뜻이 아니다. Production ES256 verifier·pinned JWKS, 정확히 한 개의
durable atomic replay adapter, Controller·Receipt/Gate/Ledger·KMS historical seal 검증이 아직 없으므로 실제
Internal 요청은 `503 TRUST_UNAVAILABLE`로 fail closed한다. Platform/Provider/Auth migration과 authoritative
OpenAPI·Generated Client, authoritative domain ownership·current-state·SoD validator, direct TLS/mTLS,
운영 key rotation·outage 증거도 전체 Release Gate로 남는다. 21개 command payload branch의 closed-field
shape validator는 receiver에 연결됐지만 실제 handler와 권한원 검증을 대신하지 않는다. Gateway의
`/api/platform/internal{,/**}` 외부 exact 404 deny는 별도 Gateway 285건과 Widget 동결 소스를 포함한 fresh
Backend 전체 72/72 Task로 검증됐다. 따라서 `ADM009-BE-101`~`ADM009-SEC-107`의 나머지 Backend Phase 1A는
번호 예약과 단일 Owner가 확정될 때까지 `BLOCKED`이며, receiver fence는 Release 면제가 아니다.

2026-08-29에는 미래 Trust adapter 3종이 우연히 정확히 하나씩 등록될 때 Receiver가 조용히 열리는 경로를
차단하기 위해 deny-only activation interlock을 추가했다. Canonical 설정
`dwp.platform.widget-registry-enabled`는 누락/`false`에서 모든 exact Receiver 요청을 기존
`503 TRUST_UNAVAILABLE`로 차단하고, 현재 Milestone의 `true`와 잘못된 Boolean은 Platform 초기화를 실패시킨다.
Filter는 조건 없이 등록되며 production permit을 여는 API·Profile·Health mutation은 없다. 실제 Spring
component scan과 wiring을 포함한 Widget 보안 78건은 2026-08-31 고정 Backend SHA
`373d233e4bcc350d454c49d1e5cdeef2b62a618f`에서 재실행해 78/78을 통과했다. 같은 Snapshot의 Widget 중앙
Registry PostgreSQL 회귀도 V207 8/8, V206 5/5, 합계 13/13을 통과했고 source-size 1,230 production file과
service-boundary도 통과했다. Platform 전체 `check`는 Widget가 아닌
`CalendarP0GovernancePostgresIntegrationTest` Testcontainer 연결 EOF로 성공 완료되지 않았으므로 과거
`860/860`을 최신 증거로 재사용하지 않는다. 이 interlock은 운영 Blocker를 해제하지 않고 잘못된 조기
활성화만 단조롭게 제한한다.

중앙 계약 감사에서 Receiver 보강 이후 생성된 `TargetContract`와
`AUTHORITY_HEADERS_FORBIDDEN`의 Registry drift를 추가로 발견해, 이미 적용된 V205는 수정하지 않고
Platform V206·V207 순방향 마이그레이션으로 수렴했다. V206은 command target 8값·typed binding 1건을,
V207은 ingress failure 12값·revision 4·typed binding 1건·catalog health `12/1/1/REGISTERED`를 강제한다.
V207 SHA-256은 `e1759a438deb1e115522b94cfacd42118da5e4e4bb52dc4bf17445c03ec06635`, Flyway
checksum은 `-1314302349`다. V205/V206/V207 PostgreSQL 18건과 Receiver·Provisioning 66건, 총 84건이
격리 Snapshot에서 통과했고, 동일 Snapshot의 Platform 전체 `check`는 774/774 Test를 통과했다. 최종
Backend 전체 `check`도 no cache·rerun·단일 worker에서 72/72 actionable Task를 fresh 실행해 6분 14초에
통과했다. Managed `dwp_platform`은 V207 적용 후 failed migration 0, 8002 clean boot와 health 200을
확인했다. 전체 code-contract audit도 770 contract·3,008 active value·904 binding 정합으로 통과했다.

### Phase 1B — Authoritative Read 전환

| ID               | 작업                                                    | 상태      |
| ---------------- | ------------------------------------------------------- | --------- |
| `ADM009-BE-151`  | Seed 5종 Effective Catalog Read-only Authoritative 전환 | `planned` |
| `ADM009-OPS-152` | Shadow Diff 0·Kill p99·Quarantine·장애 Drill            | `planned` |
| `ADM009-FE-153`  | Server Capability·Reason 기반 Library Source 연결       | `planned` |

### Phase 2 — Instance v6와 Runtime Broker

| ID               | 작업                                          | 상태      |
| ---------------- | --------------------------------------------- | --------- |
| `ADM009-BE-201`  | `definitionKey`와 persisted `instanceId` 분리 | `planned` |
| `ADM009-MIG-202` | Layout v5→v6 Dual-write·Shadow·Cutover        | `planned` |
| `ADM009-BE-203`  | Runtime Broker·Batch·Source ACL·Cache 격리    | `planned` |
| `ADM009-FE-204`  | 중앙 Renderer와 Schema 기반 Config Form       | `planned` |

### Phase 3 — Preset·Blueprint 공유

| ID               | 작업                                         | 상태      |
| ---------------- | -------------------------------------------- | --------- |
| `ADM009-BE-301`  | Widget Preset·Revision·Grant·Provenance      | `planned` |
| `ADM009-FE-302`  | Preset 생성·공유·Shared with me              | `planned` |
| `ADM009-FE-303`  | 호환성 Diff·현재 사용자 Preview·사본 적용    | `planned` |
| `ADM009-ADM-304` | Tenant Library Submission·Moderation         | `planned` |
| `ADM009-MIG-305` | 기존 HomeTemplate의 Home Blueprint 표기·호환 | `planned` |

### Phase 4 — Provider Ecosystem·Sandbox

| ID               | 작업                                           | 상태     |
| ---------------- | ---------------------------------------------- | -------- |
| `ADM009-PRV-401` | Provider Registry·Certification·Release UI     | `future` |
| `ADM009-SDK-402` | App Widget SDK·CI Validator·Signature          | `future` |
| `ADM009-SEC-403` | 별도 Origin Sandbox·CSP/SRI·Capability RPC     | `future` |
| `ADM009-OPS-404` | Ring Rollout·Migration·Global Revocation Drill | `future` |
| `ADM009-AI-405`  | Diff·승인·Undo 기반 Composer 제안              | `future` |

## 3. 요구사항 추적

| 요구사항                 | 설계                               | API·데이터                             | 수용 기준       |
| ------------------------ | ---------------------------------- | -------------------------------------- | --------------- |
| 빈 Add Dialog 개선       | `02 §2.2`                          | `03 §4.1`                              | `AT-01`~`AT-09` |
| Definition·Instance 분리 | `01 §3·§8`                         | `03 §2`                                | `AT-10`~`AT-16` |
| 3개 관리 평면            | `01 §4`                            | `03 §4`                                | `AT-20`~`AT-26` |
| 현재 사용자 Binding      | `01 §6`                            | `03 §6`                                | `AT-30`~`AT-35` |
| Lifecycle·Revocation     | `01 §7`                            | `03 §2·§7`                             | `AT-40`~`AT-47` |
| Preset 공유              | `01 §3·§4.3·§6·§7`, `02 §2.3·§2.4` | `03 §10` 의존성만 정의; DTO·API 미작성 | `AT-50`~`AT-56` |
| 접근성·반응형            | `02 §6`                            | 해당 없음                              | `AT-60`~`AT-68` |

## 4. 구현 순서와 의존성

```text
Phase 0 Frontend Catalog 의미 수정
→ Control-plane Shadow Bundle
→ Seed 5종 Authoritative Catalog Read
→ Instance v6
→ Runtime Broker
→ Private Preset
→ Direct·Group·Tenant Sharing·Moderation
→ 인증된 Code Widget·AI 확장
```

Tenant Mutation UI는 Policy API보다 먼저 활성화하지 않는다. User Preset 공유는 Definition Version,
현재 사용자 Binding, Grant·Moderation과 Revocation이 완성되기 전에 활성화하지 않는다.

## 5. Feature Flag·Rollout

후보 Flag:

- `VITE_HOME_WIDGET_LIBRARY_ENABLED`
- `widget-registry-enabled`
- `widget-catalog-shadow-enabled`
- `widget-catalog-authoritative-enabled`
- `tenant-widget-policy-enforcement-enabled`
- `widget-instance-v6-dual-write-enabled`
- `widget-instance-v6-read-enabled`
- `widget-runtime-broker-enabled`
- `widget-preset-enabled`

기본값은 모두 `false`다. Frontend Flag만으로 Mutation Capability를 추론하지 않고 서버의 Effective
Capability를 사용한다.

Rollout:

1. Storybook/Unit 수준 Static Catalog
2. 내부 Tenant Frontend-only Library
3. Definition Registry Read-only Shadow
4. Pilot Tenant의 Instance v6·Tenant Policy
5. Preset Private → Direct → Group → Tenant Library
6. Provider App 확대

## 6. 위험 Register

| 위험                                 | 중단 조건                            | 복구                                 |
| ------------------------------------ | ------------------------------------ | ------------------------------------ |
| 기존 Flow Layout Hash 변경           | Flag On/Off만으로 Preference가 변함  | Feature Flag Off, v5 Adapter Read    |
| 권한 밖 Definition·Data 노출         | 사용자 간 제목·Count·ID 노출 1건     | 전역 Runtime Kill Switch, Cache 폐기 |
| Definition 업데이트 대량 파손        | Unsupported/Migration 오류 임계 초과 | Version Pin, 이전 Version Rollback   |
| Shadow Catalog 장애가 Home 전체 차단 | App 실행·Now가 사용 불가             | Shadow 호출 중단, 정적 Runtime 유지  |
| Authoritative 장애에서 정적 Fallback | 거부 항목이 다시 보이거나 실행됨     | Fail Closed, Kill, 권한원장 복구     |
| 공유 Preset에 데이터 포함            | Payload Scan 위반                    | 게시 차단·Preset Revocation·감사     |
| Route Ledger 충돌                    | 메뉴 Checksum·총계 실패              | 기존 Surface 확장으로 복귀           |

## 7. 완료 정의

- 요구사항·화면·API·수용 ID가 구현 Issue와 연결됨
- 코드, OpenAPI, Migration, Generated Client가 동일 계약을 사용함
- 권한·부분 장애·Revocation·Rollback Contract Test 통과
- 1440·1280·390·320, 200%, Keyboard, High Contrast, Reduced Motion 증거 존재
- Pilot Baseline, Owner, On-call, Kill Switch와 복구 Runbook 지정
- 사람 승인과 운영 활성화가 기록되기 전 `release-ready` 또는 `complete`로 표시하지 않음
