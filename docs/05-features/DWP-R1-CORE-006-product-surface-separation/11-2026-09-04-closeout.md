# 앱 관리자 마감 재검증 — 2026-09-04

## 판정과 범위

중단 지점을 최신 공유 트리에서 회수한 결과, `000/100` 호환 구간에서 Work Shell을 Management
Shell로 재사용하고 APP 권한의 빈 목록을 Work 허용으로 해석하는 공통 P1을 확인했다. HCM의
Surface별 audience, Approvals·Services·HCM의 관리 Index, Approvals 로컬 404 문맥까지 함께
보정하고 집중 브라우저·타입·단위 검사와 독립 코드 재감사를 완료했다. 회사 관리 센터와 앱별
관리 워크벤치의 분리, 정확한 scope와 독립 승인자 검사, 오류 복구, 빈 상태, Preset 수명주기,
반응형·키보드 경계를 유지한다.

이번 공통 마감 범위는 **CLOSED / FROZEN**이다. 아래에 기록한 내부 차단과 추가 번들 개선을
완료했으며, 임시 검증 서버와 build 쓰기를 종료했다. 외부 증거와 신규 연동 승인은 별도다.

검증 기준은 여러 제품의 동시 미커밋 변경이 존재하는 공유 작업 트리이므로 이 기록은 immutable
release attestation이 아니다. 앱 관리자 공통 Route·Layout·권한 helper·회귀·ADR은 이번
재검증에서 실제 변경했으며, 다른 제품의 기능·API 파일과 외부 운영 증적은 소유 범위 밖으로
분리했다.

## 중단 회수 최종 증분

이번 재개에서 다음 공통 불변식을 코드와 회귀로 고정했다.

- 12개 제품 Router의 Work·Management 호환 Shell을 분리하고 공통 two-surface builder의
  `managementLegacyShell`을 필수화했다.
- governed Work Route는 APP permission 목록이 비어 있어도 fail-open하지 않는다. 서버 Work
  결정도 기대 `productId`·`surfaceId`·APP resource가 모두 같은 경우에만 호환 Guard를 통과한다.
- Management 전용 사용자는 대상 APP 권한이 없거나 다른 APP 권한만 있어도 자신의 관리
  Workbench에 들어가며 Work Route와 Work 복귀 링크는 열리지 않는다.
- HCM personal·team·operations·management는 하나의 APP Guard가 아니라 실제 audience와
  capability로 각각 판정한다. Operations의 `DATA.WORKFORCE:VIEW`와 Foundation Action 권한을
  APP entitlement로 다시 막지 않는다.
- Approvals·Services·HCM의 `/admin`·`/manage` 호환 Index는 permissions 로딩을 기다린 뒤 첫
  접근 가능 Management PAGE로 이동하고 query·hash를 보존한다. 후보가 없으면 `/403`이며 Work
  기본 화면으로 fallback하지 않는다.
- Approvals의 등록되지 않은 Work 하위 URL은 `approvals.work` Shell 안 로컬 404로 남는다.
  더 긴 `/approvals/admin` matcher가 항상 Management를 우선하므로 관리 문맥과 충돌하지 않는다.

최신 재개분의 직접 검증은 다음과 같다.

| 검사                                                                           | 결과                                  |
| ------------------------------------------------------------------------------ | ------------------------------------- |
| 앱 관리자 집중 Vitest                                                          | 8 files / 55 PASS                     |
| DWAI·ON 신규 메뉴 공통 원장 Vitest                                             | 4 files / 45 PASS                     |
| `corepack yarn test`                                                           | 426 files / 2,834 PASS                |
| `corepack yarn typecheck --incremental false`                                  | PASS                                  |
| 소유 파일 ESLint `--max-warnings=0`                                            | PASS                                  |
| `corepack yarn source-size:check`                                              | 1,661 production files PASS           |
| `product-surface-menu-separation.spec.ts`, Chromium + mobile, workers=1        | 85 PASS / 3 의도적 skip               |
| DWP Vite build + 초기 bundle budget                                            | 1,058.3 KiB raw / 308.6 KiB gzip PASS |
| `corepack yarn build:products`                                                 | 16/16 product builds PASS             |
| Product Authorization·closure·fixture·application/route/API/feature/cycle 검사 | PASS                                  |
| 계산형 내부 closure                                                            | 12/12 제품 · 60/60 PEP · COMPLETE     |
| 독립 아키텍처·보안 및 UX·접근성 재감사                                         | P0 0 / P1 0 · 동결 가능               |

최종 번들 감사에서는 HCM 관리 Guard가 동기 참조하는 `use-hcm-experience.ts`의
`@dwp-frontend/shared-utils` 루트 barrel import가 전체 API 재수출을 초기 셸에 포함시키는 P1을
추가로 찾았다. 의미와 실행 시점은 바꾸지 않고 People API·HTTP 오류·Auth hook을 기존 leaf
subpath로 직접 가져오도록 좁혔으며, root barrel 재유입을 정적 회귀로 고정했다. 이어 권한
판정만 소유하는 `hcm-surface-access.ts`를 People 조회 모듈에서 분리해 Router가 데이터 API를
초기 로드하지 않도록 했다. 보정 전 1,184.6 KiB raw / 336.5 KiB gzip이던 초기 그래프는
1,058.3 KiB / 308.6 KiB로 줄어 기존 예산을 올리지 않고 통과했다.

최신 전체 `architecture:check`는 DWAI·ON 신규 화면을 실제 Router에 연결한 뒤 권한 정본·12/12
내부 closure·fixture·application·route·API·feature·cycle·production reachability·internal
exports까지 모두 통과했다. design-system은 실제 제거된 grandfathered 사용만 baseline을 하향
고정해 통과했고 i18n·display·production source-size도 통과했다. Meeting 소유자가 E2E 파일을
1,189줄로 분리한 뒤 공통 maintenance baseline도 그 감소만 하향 고정해 통과했다. DWAI·ON
제품 소유자가 남은 포맷도 정리한 뒤 전체 `format:check`, production `build`, 16개 독립 제품
`build:products`를 재실행해 모두 통과했다. 이 로컬 Gate 통과를 외부 운영 출시 PASS로 완료
처리하지 않는다.
운영 승인·배포환경·수동 인수·침투 테스트 등 외부 증거도 `BLOCKED_EXTERNAL`을 유지한다.

## 선행 마감 snapshot — 후속 공유 트리 변경 전

아래 표는 후속 제품 개발이 시작되기 전 Node.js 24.19.0 공유 snapshot에서 실행한 이력이다.
최신 판정과 수치는 위 `중단 회수 최종 증분` 표를 사용한다.

| 검사                                                              | 결과                                      |
| ----------------------------------------------------------------- | ----------------------------------------- |
| 앱 관리자·셸 집중 Vitest                                          | 8 files / 40 PASS                         |
| 공통 메뉴·PAGE 분류 집중 Vitest                                   | 3 files / 40 PASS                         |
| 공통 권한 helper·import 경계 Vitest                               | 2 files / 14 PASS                         |
| `corepack yarn test` (메뉴·공통 import 정합 후 전체 재실행)       | 385 files / 2,606 PASS                    |
| `corepack yarn product-surfaces:readiness:test`                   | 34 PASS                                   |
| `corepack yarn typecheck --incremental false`                     | PASS                                      |
| 전체 `eslint . --max-warnings=0`                                  | PASS                                      |
| 디자인 시스템 adoption · i18n                                     | PASS                                      |
| `corepack yarn architecture:check`                                | PASS                                      |
| `corepack yarn build`                                             | PASS · 기존 번들 예산 유지                |
| 앱 관리자 3개 구현·2개 단위 파일 ESLint, `--max-warnings=0`       | PASS                                      |
| `role-governance-bootstrap.spec.ts`, Chromium + mobile, workers=1 | 25 PASS / 1 조건부 skip                   |
| `product-surfaces:closure:check`                                  | 12/12 제품 · 60/60 PEP · COMPLETE         |
| `product-surfaces:readiness:check`                                | 내부 대기 0 · 외부 출시 증적 37건 BLOCKED |
| `git diff --check`                                                | PASS                                      |

브라우저 검사는 통제된 권한·API fixture를 사용하며 실제 배포 서비스의 인수 증거는 아니다.
기존 개발 서버에 영향을 주지 않도록 새 test-mode Vite 서버
`http://127.0.0.1:4349`에서 실행했고 종료 후 listener가 없음을 확인했다.
동적 모듈 로드 오류는 오류 경계 복구 시나리오가 의도적으로 주입한 것으로 실제 실패가 아니다.
브라우저 최종 재실행은 공통 import 보정 후 48.6초, 전체 단위 검사는 51.61초에 완료됐다.
desktop의 조건부 skip 1건은 mobile 전용 320px·200% text 시나리오다.
과거 전체 build/unit 수치를 이번 공유 트리의 전체 실행 결과로 재사용하지 않는다.
이번에는 `build:products`의 16개 제품별 빌드나 backend 전체 check를 재실행하지 않았다.

### 병행 작업과 분리한 전체 Gate 상태

앱 관리자 검증 후에도 공유 트리의 제품 개발은 계속되고 있다. Meeting의 personal-room locale와
U09 source-view 판별 union 오류는 해당 소유 작업에서 해소했고, 독립 전체 non-incremental
타입 검사도 통과했다. `pages/meetings.tsx` → `MeetingFollowUps` → Work SDK의 실제 목록·상세·
수락·수행 연결도 확인했다. source promotion·reassignment 호출은 추가되지 않았다.

`work-assignment-api.ts`와 `work-assignment-contracts.ts`의 production reachability 차단은
해소됐다. 독립 검사 결과 1,152/1,155 모듈, 42 roots, 3 governed verification roots와
2,430 internal exports가 통과했다. 임시 import나 검사 예외는 추가하지 않았다.

추가로 발견한 Meeting 테스트 파일의 maintenance 크기 초과는 소유자가 예약 회귀를 전용
테스트 파일로 이동해 1,266줄로 해소했다. 공통 검사기가 감소한 한도의 고정도 요구하므로
baseline의 해당 행만 1,318 → 1,266으로 낮췄다. 다른 소유자가 변경한 baseline 행은 보존했다.
Preparation 화면의 표기는 기존 `MeetingStatusChip` 재사용으로 정합했고, 공통 display locale는
변경하지 않았다. display-dictionary 독립 재검사도 통과했다. 이어서 전체 architecture와
production build를 재실행해 PASS했다. build에 포함된 OpenAPI·권한 정본·fixture·API/feature
경계·source-size·maintenance·디자인 시스템·i18n·display·ESLint·TypeScript·Vite·번들 검사도
통과했다. 이후 별도 제품 소유자가 계속하는 기능 변경·실제 서비스 배포는 이 실행의 인증 범위가 아니다.

### 초기 번들 초과의 추가 개선

전체 build에서 초기 번들 예산 초과를 실제 재현했다. 독립 전문가와 소스맵을 대조한 결과,
초기 로딩되는 Approval 내비게이션 및 공통 권한 helper가 `shared-utils` 루트 barrel을 runtime
import하면서 다른 제품의 API 모듈까지 초기 그래프에 포함하는 것이 원인이었다.

공통 `product-surface-capability-access.ts`와 `use-product-surface-request-scope.ts`의 import를
기존 auth leaf 모듈로 좁혔다. Approval 소유자는 별도로
`approval-inbox-queue-navigation.tsx`의 API import 한 줄을 기존 approval API leaf로 정합했다.
렌더링·lazy 시점·권한 판정·메뉴·query 동작은 변경하지 않았다. 두 공통 helper가 다시 runtime
루트 barrel을 가져오지 못하도록 TypeScript AST 기반 회귀 2건도 추가했다.

| 초기 번들 | 보정 전     | 보정 후     | 기존 한도   |
| --------- | ----------- | ----------- | ----------- |
| raw       | 1,175.9 KiB | 1,050.5 KiB | 1,074.2 KiB |
| gzip      | 334.3 KiB   | 306.8 KiB   | 317.4 KiB   |
| 요청 수   | 5           | 5           | 5           |

raw 125.4 KiB, gzip 27.5 KiB를 줄였고 예산·allowlist를 늘리지 않았다.
보정 뒤 전체 단위 검사, 전체 build, 앱 관리자 Chromium·mobile 회귀까지 다시 통과했다.

### 추가 고대비 모드 후보의 판정

Meeting 소유자가 전달한 dark·forced-colors 대비 후보도 전문가가 공통 소스를 읽기 전용으로
확인했다. 일반 Typography·primary/quiet Button에는 명시적인 `WebkitTextFillColor` 고정값이
없으며, 공통 theme의 해당 선언은 input autofill에만 적용된다. 제공된 mobile/WebKit 화면은
Chromium 버튼의 실제 대비를 증명하지 않으므로 공통 P1로 확정하지 않았다.

[W3C 색상 조정 규격](https://www.w3.org/TR/css-color-adjust-1/#forced-colors-properties)은
forced-colors의 computed color와 실제 used color를 구분한다.
[WHATWG text-fill 규격](https://compat.spec.whatwg.org/#the-webkit-text-fill-color-property)의
상속·초깃값도 대조했다. 계산된 대비 수치만으로 전역 테마를 변경하지 않았고, 실제 paint와
브라우저·media 상태를 분리한 제품 회귀는 Meeting 소유 작업에 인계했다. 이 후보를
앱 관리자 미해결 결함이나 전역 대비 PASS의 증거로 사용하지 않는다.

## 후속 Meeting 관리자 호환 경계 마감

공통 마감 뒤 실제 로컬 계정 `joonbin@sk.com`의 `/meetings/admin/operations` 접근을 다시
검증했다. 거부 원인은 앱 관리자 UX의 전역 혼합이 아니라 두 개의 독립 조건이었다.

1. tenant 1의 해당 계정에는 `APP.MEETINGS` 업무 권한만 있고 `ADMIN.MEETINGS` 권한을 주는
   `MEETING_ADMIN` 역할이 없었다. 데이터 상태 기준의 403은 정상이다.
2. Meeting Router가 업무와 관리 호환 셸 모두에 `APP.MEETINGS`를 요구해, manifest의
   `meetings.management.entryAccess.requiresProductEntitlement=false`와 어긋났다.

Router는 관리 호환 셸만 `ProductRouteGuard(ADMIN.MEETINGS)`로 분리했다. 업무 셸은 기존
`AppRouteGuard(APP.MEETINGS)`를 유지하고, 관리 PAGE 3개는 계속 DRAFT다. 공통 canary 경계,
immutable v1-v4, official PAGE registry, rollout flag와 readiness 증거는 수정하지 않았다.
따라서 `110/111`에서 관리 exact authority가 없을 때의 fail-closed도 유지된다.

로컬 사용자 여정은 raw SQL이나 migration으로 우회하지 않았다. Directory Admin API로 LOCAL
`SKAX_MEETING_ADMINS` 그룹의 기존 두 구성원을 보존한 채 user 900018만 추가했다. 이 경로가
그룹 revision `1 → 2`, version `0 → 1`, 사용자 access revision 갱신과 기존 세션 폐기를
수행했다. fresh login에서 `ADMIN.MEETINGS:VIEW/UPDATE/MANAGE`와 `MEETING_ADMIN`, Meeting
rollout `100/NOT_EVALUATED`를 확인했고, overview·policy·intelligence 조회 API는 모두 200이었다.
이는 로컬 개발 검증용 가역 상태이며 운영 역할 부여나 출시 승인이 아니다. 원복 정보는 repo 밖
`/tmp/dwp-meeting-admin-grant-tenant1-user900018-20260904.json`에 기록했다.

checker가 없는 격리 Vite `http://127.0.0.1:4351`에서 실제 Gateway·Auth·Meeting 서비스를
사용해 desktop 1440px와 mobile 390px를 각각 확인했다. 두 환경 모두 업무 화면의 단일
`앱 관리: 화상회의` 진입점이 `/meetings/admin/operations`로 이동하고, `관리 모드`,
`업무로 돌아가기`, 관리 메뉴 3개만 표시됐다. 업무 메뉴는 관리 sidebar에 나타나지 않았으며
policies·intelligence deep link의 H1과 URL도 일치했다. 검증 서버는 종료했다.

집중 Vitest 27/27, 두 파일 ESLint 0 warning, Prettier, 전체 non-incremental TypeScript가
통과했다. 별도 Work 제품 개발이 동시에 추가한 미연결 production module은 전체
`architecture:check`의 외부 차단으로 분리해 Work 소유 작업에 인계했다.

Meeting 관리 Surface를 `110/111`로 정식 승격하려면 append-only 후속 bundle에서 관리
PAGE 3종, 정책 갱신 ACTION, `meetings.operations.read`·`meetings.policy.read`·
`meetings.policy.update`, canonical `RESOURCE_SET`, Gateway·Meeting owner PEP와 외부 소유자
승인·immutable attestation을 함께 닫아야 한다. permission-only 사용자를 tenant-wide exact
책임자로 자동 승격하거나 DRAFT를 client에서 우회하지 않는다.
개발 가능한 계약과 안전 Gate는 [Meetings 관리 Surface 후속 Bundle 승격 설계](12-meetings-management-next-bundle-activation-design.md)에
고정했다. 이 설계 문서의 `DESIGN READY`는 production activation 승인이나 bundle 생성을 의미하지 않는다.

## 신규 사용자 메뉴의 공통 정본 정합

제품 소유자가 추가한 `mail.contacts` → `/mail/contacts`, `meetings.follow-ups` →
`/meetings/follow-ups`, `meetings.templates` → `/meetings/templates`, `meetings.preferences` →
`/meetings/preferences`와 DWAI·ON의 `dwaion.routines` → `/dwaion/routines`,
`dwaion.personal-controls` → `/dwaion/personal-controls`, `dwaion.artifacts` →
`/dwaion/artifacts`를 실제 manifest와 대조했다. 모두 기존 업무 Surface·Policy를 사용하는
`W3`, `work/work`, ACTIVE 메뉴이자 DRAFT PAGE다. DWAI·ON의 기존 15개 메뉴는 `W2`를 유지하고
확장 세 메뉴만 항목 단위 `W3`로 분류한다.
Meeting 관리 메뉴 3개는 변하지 않고 예약·준비·개인 회의실 contextual view는 정적 메뉴에
추가하지 않았다.

[메뉴 분류표](../../03-architecture/R1%20제품%20Surface%20전체%20메뉴%20분류표.md)는
전체 193개, 업무 앱 146개, DWAI·ON 18개, Mail 16개, Meeting 10개로 정합화했다. DRAFT PAGE
91개와 공식 PAGE 66개를 합친 브라우저 PAGE는 157개다. Authorization Registry v5의 기존
160개(PAGE 66, DATA 27, ACTION 67)는 변경하지 않았다.

메뉴 체크섬은 `a718725494d965dc85f031544c1d04f07824656eb7cef5e6b9bd206b10d4fee6`이다.
직전 190개 원장 체크섬 `cd0fea0d21ee314d080435cbc12f3fd72852839129260b27e29d0b2d0ac8fbdf`도
lineage 회귀로 보존해, 사용자 메뉴 추가가 다른 메뉴의 identity·path·plane·surface·wave drift를
가리지 못하도록 했다.

## 새로 회수한 공통 정적 차단

Work 소유 클라이언트의 `POST /internal/v1/meeting-followups/resolve`가 기존 서비스 경계
검사기의 단일 파일 path marker 및 세 가지 service-token 가정 때문에 등록되지 못했다.
공통 검사기에 별도 `signedWorkload` 프로파일을 추가하고 실제 protocol·signer 소스와 연결했다.

- 기존 token 기반 인터페이스의 경로·인증·trace 조건은 유지한다.
- protocol 상수와 실제 import·endpoint·POST bytes·서명·subject/source/action·30초 만료·nonce를 확인한다.
- 다른 서비스 소스, 경로 탈출, 잘못된 client 경로, 주석·문자열 위조 marker를 거부한다.
- 만료 산술 확장, Gateway/legacy token 재사용, 자동 retry 및 redirect 허용을 거부한다.
- 검사는 정적 계약 검사다. 암호학적 실행, receiver replay 방지, 현재 사용자 권한의 증명으로 간주하지 않는다.

backend `scripts/tests/test_service_boundaries.py`의 18개 회귀와 전체 서비스 경계 검사를 통과했다.
`./gradlew checkServiceBoundaries --no-daemon --max-workers=1`도 BUILD SUCCESSFUL이며,
독립 재감사에서 지적한 경로 검증·만료 산술·redirect 위장 회귀를 포함한다.
Work production 코드는 Work 소유 작업이 유지하며, 실제 trace 전파·민감 헤더 비전달 HTTP
검증은 해당 소유 작업의 [구현 보고서](../DWP-R1-WRK-001-unified-work-execution/meeting-assignment/implementation-report.md)에 기록한다.

## 이번 작업의 소유 파일

공유 트리에 여러 제품의 미커밋 변경이 함께 존재하므로 파일 전체가 아니라 아래 책임 범위를
이번 앱 관리자 마감의 소유 단위로 삼는다. 제품 비즈니스 화면·데이터 API 동작과 불변 Product
Authorization Bundle은 수정하지 않았다.

- `apps/dwp/src/components/product-surface-capability-access.ts`: 기존 auth leaf import만 사용
- `apps/dwp/src/components/use-product-surface-request-scope.ts`: 기존 auth leaf import만 사용
- `apps/dwp/src/components/use-product-surface-request-scope.test.ts`: runtime barrel 재유입 회귀
- `apps/dwp/src/routes/product-menu-manifest.test.ts`
- `apps/dwp/src/routes/product-surface-expansion.test.ts`
- `apps/dwp/src/routes/product-route-contracts.generated.test.ts`
- `apps/dwp/src/routes/meetings-routes.tsx`: 업무 entitlement와 관리 authority 호환 셸 분리
- `apps/dwp/src/routes/meetings-routes.test.tsx`: 셸 분리와 관리 PAGE DRAFT 불변 회귀
- `apps/dwp/src/routes/two-surface-product-routes.tsx`: 독립 `managementLegacyShell` 필수 계약
- `apps/dwp/src/routes/{approvals,calendar,communications,dwaion,hcm,mail,messaging,notification,rooms,services,spaces}-routes.tsx`:
  Work·Management 호환 Guard와 Shell 분리
- `apps/dwp/src/routes/legacy-product-first-allowed-index.tsx` 및 `.test.ts`: 로컬 관리 권한 기반
  첫 PAGE 선택, loading·query·hash·fail-closed 계약
- `apps/dwp/src/routes/route-support.tsx` 및 `.test.ts`: exact product·surface·APP 결속의 strict Work Guard
- `apps/dwp/src/routes/product-management-legacy-shells.test.tsx`: 12개 제품 호환 Shell 정적 회귀
- `libs/shared-utils/src/auth/app-entitlements.ts` 및 `.test.ts`: 명시적 APP entitlement 판정
- `apps/dwp/src/features/shell/legacy-product-surface-presentation.ts` 및 해당 Route 회귀:
  접근 불가능한 반대 Surface 전환·복귀 비노출
- `apps/dwp/src/layouts/product-area-layout.tsx`, `apps/dwp/src/layouts/hcm-layout.tsx`,
  `apps/dwp/src/features/hcm/hcm-surface-access.ts`, `apps/dwp/src/features/hcm/use-hcm-experience.ts`,
  `apps/dwp/src/routes/hcm-legacy-access.test.ts`: Surface audience 기반 표시·진입 계약과
  권한/People 조회의 초기 번들 경계
- `apps/dwp/src/features/approvals/approval-product-manifest.ts` 및 `.test.ts`: Approvals Work 하위 로컬
  not-found와 Management longest-match 계약
- `e2e/product-surface-menu-separation.spec.ts`: zero/unrelated APP, management-only,
  Chromium·mobile 통합 회귀
- `scripts/maintenance-source-size-baseline.json`: 검증된 `e2e/video-meetings.spec.ts` 축소를
  1,189줄로 하향 고정
- `scripts/design-system-adoption-baseline.json`: 실제 제거된 grandfathered 사용만 하향 고정
- `scripts/check-product-surface-production-readiness.mjs`, 해당 `.test.mjs`,
  `scripts/product-surface-readiness-authorization.mjs`,
  `scripts/check-product-surface-production-readiness.test-support.mjs`: 공개 CLI와 신뢰 경계를
  유지한 readiness 권한·테스트 지원 책임 분리
- `docs/03-architecture/R1 제품 업무·관리 Surface 분리 및 관리 Context ADR.md`
- `docs/03-architecture/R1 제품 Surface 전체 메뉴 분류표.md`
- `docs/05-features/DWP-R1-CORE-006-product-surface-separation/README.md`
- 이 마감 기록 `11-2026-09-04-closeout.md`

Backend 공통 소유는 다음 4개다. Work·Meeting production 구현은 각각의 소유 작업이 유지한다.

- `scripts/check-service-boundaries.py`
- `scripts/tests/test_service_boundaries.py`
- `docs/architecture/service-interface-contracts.json`
- `docs/architecture/service-interface-boundary-and-gateway-policy.md`

## 완료와 분리해야 하는 새 연동 요구

Meeting→Work 원천 전환은 현재 **NO-GO**다. 서비스 간 서명은 호출자와 요청 본문을 증명하지만
그 사용자의 현재 Meeting 이용 권한이나 새 업무 담당자의 적격성을 대신하지 않는다.

1. Auth: Meeting 전용 인증·audience의 좁은 평가 포트가 필요하다. 기존 Gateway 전용 평가
   API나 자격 증명을 재사용하지 않는다. 현재 계정·tenant 상태, durable identity plane,
   entitlement, exact capability, scope 및 revision을 서버가 재조회해야 한다.
2. 공통 계약: 현 v4의 회의 생성 권한을 후속 업무 전환·재배정 권한으로 대체하지 않는다.
   다음 additive 후보에서 internal-only binding과 본문 action을 표현하고, 정확한 permission
   매핑·역할 부여·risk tier를 승인해야 한다. 기존 v1~v4 바이트·checksum은 유지한다.
3. People: 현재 HCM actor/scope 평가 API는 target assignee 적격성 API가 아니다.
   Auth user↔People person 결속과 최신 lifecycle을 확인하는 독립 대상 판정 계약이 필요하다.
4. Meeting: 위 권한에 현재 report/candidate ACL, 사람의 확정, retention, source version을
   함께 적용한다. Work sourceVersion, authRevision, Work version을 서로 대체하지 않는다.

공통 검토 결과는 `화상회의 앱 완성형 고도화`와 `업무 앱 역할과 차별성 분석`에 전달했고,
People owner는 `역할거버넌스 UIUX 개선` 작업으로 명시했다. 해당 작업들도 합의 전 활성화
금지와 NO-GO를 수용했다. 앱 관리자 마감을 이 새 기능의 완료로 확대 해석하지 않는다.

운영 승인·배포환경·수동 인수·침투 테스트 등 외부 37건도 별도 증거가 필요하다.
이번 마감에서 운영 역할 부여, 운영 활성화, 커밋·푸시는 수행하지 않는다. 위에 명시한 LOCAL
그룹 구성원 추가는 실제 로컬 사용자 여정 검증만을 위한 가역 개발 상태이며 출시 증거가 아니다.
