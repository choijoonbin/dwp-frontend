{
  "name": "dwp-meetings",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/product-runtime/src",
  "tags": ["scope:meetings", "type:product", "runtime:browser", "deployment:independent"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "inputs": ["meetingsProduct"],
      "options": {
        "command": "node scripts/build-product-app.mjs meetings"
      },
      "outputs": ["{workspaceRoot}/dist/apps/meetings"]
    }
  }
}
