{
  "name": "dwp-services",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/product-runtime/src",
  "tags": ["scope:services", "type:product", "runtime:browser", "deployment:independent"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "inputs": ["servicesProduct"],
      "options": {
        "command": "node scripts/build-product-app.mjs services"
      },
      "outputs": ["{workspaceRoot}/dist/apps/services"]
    }
  }
}
