{
  "name": "dwp-rooms",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/product-runtime/src",
  "tags": ["scope:rooms", "type:product", "runtime:browser", "deployment:independent"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "inputs": ["roomsProduct"],
      "options": {
        "command": "node scripts/build-product-app.mjs rooms"
      },
      "outputs": ["{workspaceRoot}/dist/apps/rooms"]
    }
  }
}
