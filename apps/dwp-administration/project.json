{
  "name": "dwp-administration",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/product-runtime/src",
  "tags": ["scope:administration", "type:product", "runtime:browser", "deployment:independent"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "inputs": ["administrationProduct"],
      "options": {
        "command": "node scripts/build-product-app.mjs administration"
      },
      "outputs": ["{workspaceRoot}/dist/apps/administration"]
    }
  }
}
