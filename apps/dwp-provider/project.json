{
  "name": "dwp-provider",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/product-runtime/src",
  "tags": ["scope:provider", "type:product", "runtime:browser", "deployment:independent"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "inputs": ["providerProduct"],
      "options": {
        "command": "node scripts/build-product-app.mjs provider"
      },
      "outputs": ["{workspaceRoot}/dist/apps/provider"]
    }
  }
}
