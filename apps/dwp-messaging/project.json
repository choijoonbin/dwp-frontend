{
  "name": "dwp-messaging",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/product-runtime/src",
  "tags": ["scope:messaging", "type:product", "runtime:browser", "deployment:independent"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "inputs": ["messagingProduct"],
      "options": {
        "command": "node scripts/build-product-app.mjs messaging"
      },
      "outputs": ["{workspaceRoot}/dist/apps/messaging"]
    }
  }
}
