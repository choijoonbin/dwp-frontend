{
  "name": "dwp-calendar",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/product-runtime/src",
  "tags": ["scope:calendar", "type:product", "runtime:browser", "deployment:independent"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "inputs": ["calendarProduct"],
      "options": {
        "command": "node scripts/build-product-app.mjs calendar"
      },
      "outputs": ["{workspaceRoot}/dist/apps/calendar"]
    }
  }
}
